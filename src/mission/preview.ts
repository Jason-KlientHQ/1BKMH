/** Re-export computeMission as the canonical mission calculator (B3). */
export { computeMission, missionPreview, formatDuration, type MissionPreview } from "@/mission/compute";
export type { MissionResult, MissionLeg } from "@/mission/types";